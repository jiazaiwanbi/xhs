# 面试版：这个项目里 MySQL、Redis、RabbitMQ 到底是怎么设计的

这篇文章只基于当前仓库代码来写，不凭经验补答案。凡是代码里能确认的，我会明确说“已实现”；凡是简历里写了但代码里没有看到完整落地的，我会明确说“代码中未看到”。

核心代码入口主要在：

- `backend/internal/http/router.go`
- `backend/internal/db/db.go`
- `backend/internal/feed/service.go`
- `backend/internal/video/*.go`
- `backend/internal/social/*.go`
- `backend/internal/middleware/redis/*.go`
- `backend/internal/middleware/rabbitmq/*.go`
- `backend/internal/worker/*.go`

---

## 1. 先说结论：这三个中间件分别负责什么

### 1.1 MySQL 负责什么

MySQL 是这个项目的最终业务数据落库点，负责保存：

- 用户账号：`accounts`
- 视频内容：`videos`
- 点赞关系：`likes`
- 评论内容：`comments`
- 关注关系：`socials`
- 发布视频的本地消息表：`outbox_msgs`
- 标签与视频标签关系：`tags`、`video_tags`
- 私信：`messages`
- 通知：`notifications`

它承担的是“最终一致的业务真相”。Redis 更多是缓存和排行榜，RabbitMQ 更多是异步事件传递，真正能作为业务事实依据的还是 MySQL。

### 1.2 Redis 负责什么

Redis 在这个项目里不是单一用途，而是同时承担了 8 类职责：

1. token 校验缓存
2. refresh token 辅助查询缓存
3. 视频详情缓存
4. 评论列表缓存
5. Feed 读模型缓存与视频实体缓存
6. 全局最新流时间线 ZSET、热榜 ZSET
7. 限流计数器
8. 分布式互斥锁

也就是说，这个项目里 Redis 既是缓存，又是排行榜，又是时间线，又是限流器。

### 1.3 RabbitMQ 负责什么

RabbitMQ 在这个项目里主要承担“异步解耦”和“事件扇出”两类职责：

- 点赞事件异步落库
- 评论事件异步落库
- 热度增量异步写 Redis 热榜
- 视频发布通过 outbox 转发到最新流时间线
- 点赞 / 评论 / 关注事件同时扇出到通知队列，做 SSE 实时通知

但要注意：

- 点赞、评论的“主写链路”确实是 MQ 异步优先，失败再降级直写
- 关注这条链路不是纯异步，代码里是“发 MQ + 同步写库同时做”

这一点面试时一定要说准。

---

## 2. MySQL 设计详解

### 2.1 表结构与职责

#### 账号表 `accounts`

字段重点：

- `id`
- `username`，唯一
- `password`，存 bcrypt hash
- `token`
- `refresh_token`
- `avatar_url`
- `bio`

这个设计说明项目没有做独立的 token 黑名单表，而是把“当前有效 access token / refresh token”直接存在用户表里，再结合 Redis 做加速校验。

#### 视频表 `videos`

字段重点：

- `author_id`
- `username`
- `title`
- `description`
- `play_url`
- `cover_url`
- `create_time`
- `likes_count`
- `popularity`

这张表里直接冗余了 `likes_count` 和 `popularity`，说明项目不是实时 `COUNT(*)` 聚合，而是把统计值作为写时维护字段，读的时候直接用。

#### 点赞表 `likes`

字段重点：

- `video_id`
- `account_id`
- `created_at`

关键点是 `(video_id, account_id)` 做了联合唯一索引，所以“一个用户不能重复点赞同一个视频”是由数据库约束兜底的，不只靠业务判断。

#### 评论表 `comments`

字段重点：

- `username`
- `video_id`
- `author_id`
- `content`
- `created_at`

评论表保存了评论时的用户名快照，而不是每次都 join `accounts` 表实时查名称。

#### 关注表 `socials`

字段重点：

- `follower_id`
- `vlogger_id`

`(follower_id, vlogger_id)` 有联合唯一索引，用来保证不能重复关注。

#### 通知表 `notifications`

字段重点：

- `recipient_id`
- `sender_id`
- `type`
- `target_id`
- `content`
- `is_read`
- `created_at`

这说明通知不是只做内存 SSE 推送，而是“先落通知表，再推实时消息”，所以用户即使离线，回来也能拉历史通知。

#### 本地消息表 `outbox_msgs`

字段重点：

- `video_id`
- `event_type`
- `create_time`
- `status`

它只服务于“视频发布 -> 最新流时间线更新”这条链路，属于 outbox 模式。

### 2.2 索引设计体现了哪些查询意图

这个项目 MySQL 的一个亮点是，视频表上的索引不是随便建的，而是明显对应 Feed 查询模式：

- `create_time` 降序索引：服务最新流
- `(likes_count, id)` 排序：服务点赞榜稳定分页
- `(popularity, create_time, id)` 排序：服务热榜 DB 降级查询

关注表上也有针对查询方向的索引：

- `follower_id`：查“我关注了谁”
- `vlogger_id`：查“谁关注了我”
- `(follower_id, vlogger_id)` 唯一：防重复关注

### 2.3 为什么说这个项目把 MySQL 当成最终真相源

因为无论 Redis 还是 RabbitMQ，最终都只是辅助：

- Redis 丢了，可以回源 MySQL
- RabbitMQ 失败了，会降级同步写 MySQL
- token 校验 Redis 未命中时，也回退到 MySQL 查 `account.token`

所以如果面试官问“谁是最终一致性的落点”，答案就是 MySQL。

### 2.4 这个 MySQL 设计的几个真实特点

#### 特点 1：没有显式外键约束

代码里是 GORM AutoMigrate 自动建表，但模型没有写外键约束。也就是说：

- 表之间主要靠业务代码维护关系
- 优点是开发简单、写入灵活
- 缺点是数据库层面不会帮你强约束引用完整性

#### 特点 2：写扩散是业务代码维护，不是触发器维护

比如点赞以后：

- `likes` 表要插入一条关系
- `videos.likes_count` 要加 1
- `videos.popularity` 要加 1

这些都不是 DB trigger 干的，而是 Worker/Service 在业务层做的。

#### 特点 3：关注流并没有落“用户时间线表”

代码中没有看到类似 `user_timeline`、`fanout_feed` 这样的表。关注流当前实现就是：

- 先查 `socials` 得到我关注的人
- 再查这些人的视频
- 最后把结果做 Redis 页面缓存

所以这不是典型的“写扩散关注收件箱模型”。

---

## 3. Redis 设计详解

## 3.1 Redis 里到底存了什么

### 1. token 缓存

- Key：`v1:account:<accountID>`
- 类型：String
- Value：access token 字符串
- TTL：24 小时

用途：

- JWT 中间件先查 Redis
- 命中且 token 一致，直接放行
- 未命中或 Redis 不可用，再回源 MySQL 查 `accounts.token`
- 查到后再回填 Redis

这是一个很典型的“缓存加速 + 自愈回填”设计。

### 2. refresh token 缓存

- Key：`v1:account:<accountID>:refresh`
- 类型：String
- Value：refresh token
- TTL：7 天

- Key：`v1:refresh:<refreshToken>`
- 类型：String
- Value：accountID
- TTL：7 天

用途：

- 刷新 access token 时，先通过 `refresh:<token>` 快速定位用户
- 再回到 MySQL 确认 `accounts.refresh_token` 是否一致

这不是“只信 Redis”，而是 Redis 做索引加速，MySQL 做最终校验。

### 3. 用户主页缓存

- Key：`v1:account:profile:<accountID>`
- 类型：String(JSON)
- TTL：5 秒

缓存内容是聚合后的主页信息：

- 基础账号信息
- 视频数
- 总获赞数
- 粉丝数
- 关注数

### 4. 视频详情缓存

- Key：`v1:video:detail:id=<videoID>`
- 类型：String(JSON)
- TTL：5 分钟

这是典型的详情页缓存。

并且前面还有一层本地缓存：

- 本地缓存 TTL：3 秒

所以视频详情是：

- L1 本地缓存
- L2 Redis
- L3 MySQL

### 5. 评论列表缓存

- Key：`v1:comment:list:video=<videoID>`
- 类型：String(JSON)
- TTL：5 秒

评论发布和删除后会主动删掉这个 key，下次重新查库回填。

### 6. Feed 视频实体缓存

- Key：`v1:video:entity:<videoID>`
- 类型：String(JSON)
- TTL：1 小时

这个缓存用于 Feed 场景批量拿视频实体，配合本地缓存一起构成：

- L1 本地缓存 5 秒
- L2 Redis 1 小时
- L3 MySQL 批量查询

### 7. Feed 读模型缓存

- Key：`v1:feed:item:<videoID>`
- 类型：String(JSON)
- TTL：1 小时

缓存内容不是完整 `Video`，而是 Feed 返回需要的展示模型：

- 作者信息
- 标题/描述
- 播放地址/封面
- 发布时间
- likes_count
- popularity

这比每次都从多张表动态组装要更适合 Feed 接口。

### 8. 全局最新流时间线

- Key：`v1:feed:global_timeline`
- 类型：ZSET
- member：`videoID`
- score：`create_time.UnixMilli()`
- TTL：无显式 TTL

这个 ZSET 是“最新流”的热数据时间线。

它不是发视频时直接同步写进去，而是：

1. 发布视频时先写 `outbox_msgs`
2. Outbox Poller 扫表
3. 发 `video.timeline.publish` 消息
4. Timeline Consumer 消费后写入 `feed:global_timeline`

另外，这个 ZSET 会做裁剪，只保留最近 1000 条。

### 9. 热榜分钟桶

- Key：`v1:hot:video:1m:<yyyyMMddHHmm>`
- 类型：ZSET
- member：`videoID`
- score：该分钟内热度增量
- TTL：2 小时

每次点赞、取消点赞、评论发布，都会让对应视频的热度变化写到“当前分钟桶”里，而不是直接写一张全局热榜大 key。

这样做的好处是：

- 降低单 key 热点竞争
- 支持滑动窗口聚合
- 更适合后续做“最近 1 小时热榜”

### 10. 热榜快照

- Key：`v1:hot:video:merge:1m:<as_of>`
- 类型：ZSET
- 来源：最近 60 个分钟桶 `ZUNIONSTORE`
- TTL：2 分钟

它的用途不是长期保存，而是给同一个分页会话提供稳定视图。

也就是说热榜第一页和第二页不会因为榜单实时变化而完全错乱。

### 11. 关注流页面缓存

- Key：`v1:feed:listByFollowing:limit=<n>:accountID=<uid>:before=<ts>`
- 类型：String(JSON)
- TTL：24 小时

这点一定要注意，代码里真实 TTL 是 24 小时，不是 5 秒。

这意味着：

- 关注流当前是“查库结果页缓存”
- 不是“用户时间线预分发”
- 而且如果不主动失效，会有较强陈旧性

### 12. 限流计数器

Redis 还承担了限流：

- 登录：每 IP 每分钟 10 次
- 注册：每 IP 每小时 5 次
- 点赞写：每账号每分钟 30 次
- 评论写：每账号每分钟 10 次
- 关注写：每账号每分钟 20 次

实现方式不是简单 `INCR` 后单独 `EXPIRE`，而是 Lua 脚本：

- 第一次 `INCR` 到 1 时同时 `PEXPIRE`
- 避免并发下计数和过期分离造成脏窗口

### 13. 分布式互斥锁

Redis 还承担了缓存击穿保护：

- 视频详情缓存 miss 时，抢 `lock:<cacheKey>`，锁 TTL 2 秒
- 关注流缓存 miss 时，抢 `lock:<cacheKey>`，锁 TTL 500ms

加锁成功者回源构建缓存，其他请求短暂等待别人回填。

## 3.2 为什么说这个项目 Redis 是“多级缓存体系”的核心

因为它不是只在一个接口上用了缓存，而是多处都用了不同层次的读优化：

- 本地缓存：抗极短时间重复访问
- Redis：抗跨实例热点访问
- MySQL：最终回源

比如视频详情就是标准三级缓存；Feed 场景则既有实体缓存，又有读模型缓存，又有 ZSET 时间线和热榜。

## 3.3 Redis 相关的面试高频问题

### 问：这个项目里 Redis 主要用到了哪里？

答：

- token 校验
- refresh token 索引
- 用户主页短缓存
- 视频详情三级缓存
- 评论列表缓存
- Feed 视频实体缓存
- Feed 读模型缓存
- 最新流全局时间线 ZSET
- 热榜分钟桶和快照 ZSET
- 限流计数器
- 分布式锁

### 问：过期时间怎么设置的？

答：

- access token 缓存 24h
- refresh token 缓存 7d
- 用户主页缓存 5s
- 评论列表缓存 5s
- 视频详情缓存 5m
- Feed 视频实体缓存 1h
- Feed 读模型缓存 1h
- 热榜分钟桶 2h
- 热榜快照 2m
- 关注流页面缓存 24h
- 本地缓存通常 2s、3s、5s

### 问：为什么热榜用 ZSET？

答：

因为热榜天然需要“按 score 排序取 TopN”。这个项目里 ZSET 的两个层次分别解决两个问题：

- 分钟桶 ZSET：解决高频写入和滑动窗口聚合
- 快照 ZSET：解决分页稳定性

### 问：为什么不直接把热度写在一个 ZSET 里？

答：

如果只维护一个大热榜 key，所有点赞评论都打在同一个 key 上，热点竞争更重，而且没法自然表达“最近 1 小时热度”。按分钟分桶后，再按窗口聚合，扩展性更好。

### 问：Redis 宕机会怎样？

答：

- token 校验会回退到 MySQL
- 点赞评论如果 MQ 失败，还能同步落库
- 视频详情和评论列表可以直接查库

但也要实话实说：

- `listLatest` 这条最新流代码强依赖 Redis 全局时间线
- Feed 内部有一些路径默认假设 Redis 存在

所以“Redis 完全可拔掉且所有 Feed 正常”这句话，代码层面不能这么答。

---

## 4. RabbitMQ 设计详解

## 4.1 RabbitMQ 拓扑有哪些

### 点赞事件

- Exchange：`like.events`
- Queue：`like.events`
- Binding Key：`like.*`
- Routing Key：
  - `like.like`
  - `like.unlike`

消费者：

- `LikeWorker`

### 评论事件

- Exchange：`comment.events`
- Queue：`comment.events`
- Binding Key：`comment.*`
- Routing Key：
  - `comment.publish`
  - `comment.delete`

消费者：

- `CommentWorker`

### 关注事件

- Exchange：`social.events`
- Queue：`social.events`
- Binding Key：`social.*`
- Routing Key：
  - `social.follow`
  - `social.unfollow`

消费者：

- `SocialWorker`

### 热度事件

- Exchange：`video.popularity.events`
- Queue：`video.popularity.events`
- Binding Key：`video.popularity.*`
- Routing Key：
  - `video.popularity.update`

消费者：

- `PopularityWorker`

### 最新流时间线事件

- Exchange：`video.timeline.events`
- Queue：`video.timeline.update.queue`
- Binding Key：`video.timeline.*`
- Routing Key：
  - `video.timeline.publish`

消费者：

- `StartConsumer` 中的 timeline consumer

### 通知扇出队列

这里有一个很容易讲出亮点的点：通知不是单独业务主动再发一次消息，而是复用已有业务 exchange 再绑定一个通知队列。

具体是：

- `like.events` 再绑定 `notification.like`，routing key `like.like`
- `comment.events` 再绑定 `notification.comment`，routing key `comment.publish`
- `social.events` 再绑定 `notification.social`，routing key `social.follow`

对应消费者是 `NotificationWorker`。

也就是说：

- 同一个点赞事件
- 一份给 `LikeWorker` 落业务数据
- 一份给 `NotificationWorker` 做通知

这就是典型的事件扇出。

## 4.2 RabbitMQ 是怎么保证可恢复性的

有 3 个点：

### 1. 持久化消息

发布 JSON 消息时使用了：

- `DeliveryMode: Persistent`

### 2. 死信交换机

声明普通队列时都挂了：

- `x-dead-letter-exchange: dlx.events`

并且会为每个队列声明 `<queue>.dlx` 死信队列。

### 3. 重试次数上限

Worker 通过 `x-death` 头读取重试次数，最大重试次数是 3。

超过 3 次后：

- 不再继续重试
- 直接 ACK，让消息进入死信链路

## 4.3 Worker 如何消费

Worker 进程会：

- 连接 MySQL
- 连接 Redis
- 连接 RabbitMQ
- 设置 `Qos(50, 0, false)`
- 启动多个 goroutine 分别消费 social、like、comment、popularity 队列

这里的意思是：

- RabbitMQ 真正承担了异步消费职责
- API 和 Worker 是可拆分部署的

## 4.4 RabbitMQ 在业务上到底用在哪里

### 1. 点赞

点赞请求到来后，服务层先尝试做两件事：

- 发 `like.events`
- 发 `video.popularity.events`

如果两者都成功，接口直接返回，真正写库由 Worker 异步做。

后续：

- `LikeWorker` 插入 `likes`
- `videos.likes_count + 1`
- `videos.popularity + 1`
- 更新 Feed 读模型中的 `likes_count`

如果发 MQ 失败：

- 同步事务写 MySQL
- 同步更新 Redis 热榜分钟桶

所以点赞链路是“异步优先，失败降级同步”。

### 2. 取消点赞

逻辑与点赞对称：

- 发 `like.unlike`
- 发热度 `-1`
- Worker 删除点赞关系
- `likes_count - 1`
- `popularity - 1`

### 3. 评论发布

评论发布时：

- 发 `comment.publish`
- 发 `video.popularity.update`，增量 `+1`

Worker 消费后：

- 写 `comments`
- `videos.popularity + 1`

如果 MQ 发失败：

- 同步写评论
- 同步更新视频热度
- 同步更新 Redis 热榜分钟桶

### 4. 评论删除

评论删除会发 `comment.delete`，由 `CommentWorker` 删除评论。

注意一个真实细节：

- 评论删除不会把 `popularity` 减回去

所以如果面试官问“热度如何计算”，你必须按代码答：当前实现是“点赞 +1、取消点赞 -1、评论发布 +1”，评论删除不回滚热度。

### 5. 视频发布 -> 最新流时间线

这条链路不是直接发 MQ，而是用了 outbox：

1. 事务里同时写 `videos` 和 `outbox_msgs`
2. 后台轮询 `outbox_msgs status=pending`
3. 投递 `video.timeline.publish`
4. timeline consumer 写入 `feed:global_timeline`
5. 成功后删除 outbox 记录

这个设计解决的是：

- 视频发出去了，但 MQ 当时不可用
- 或者数据库写成功后服务崩了

至少能保证数据库里有 outbox 记录，后面还能补投。

### 6. 实时通知

RabbitMQ 在通知场景里承担的是“事件广播总线”的角色。

比如点赞：

1. 用户点赞
2. API 发布 `like.like`
3. `LikeWorker` 处理业务落库
4. `NotificationWorker` 处理通知落表和 SSE 推送

评论、关注同理。

## 4.5 关注这条链路要怎么实话实说

这是简历里最容易被追问穿的地方之一。

代码现状不是“关注完全异步化”，而是：

- `SocialService.Follow` 会先发 `social.follow`
- 然后直接同步 `repo.Follow`
- `SocialWorker` 又会消费同一条消息再尝试写一次

为什么没出错？

- 因为数据库联合唯一索引兜底
- Worker 里也把 1062 重复键当成幂等成功

所以面试时正确说法应该是：

- RabbitMQ 在关注场景更多承担事件广播和通知扇出
- 不是纯粹依赖 MQ 异步完成主业务写入

这一点不要说成“关注关系完全异步落库”。

---

## 5. 点赞、评论、通知三条核心链路怎么答

## 5.1 点赞怎么实现

可以这样答：

1. 先做参数校验和登录校验
2. 查视频是否存在
3. 查当前用户是否已经点赞
4. 优先异步发布两类事件：
   - 点赞业务事件
   - 热度增量事件
5. 如果 MQ 发布成功，接口直接返回
6. LikeWorker 异步插入点赞关系，并原子更新 `likes_count`、`popularity`
7. 如果 MQ 发布失败，降级为同步事务写库
8. Redis 热榜分钟桶同步 `ZINCRBY`
9. Feed 读模型里的 `likes_count` 也会更新

为什么这么设计：

- 主流程更快
- 可以削峰
- 高并发下不让每个请求都直接压 MySQL

如何防重复点赞：

- 业务层先查是否已点赞
- 数据库层 `(video_id, account_id)` 联合唯一索引兜底
- Worker 侧重复消费时也会识别重复键并视为幂等

## 5.2 评论怎么实现

评论发布链路和点赞类似，但有两个差别：

- 评论本身要写内容到 `comments`
- 评论会触发 `@username` 提及解析

流程可以这样答：

1. 校验视频是否存在
2. 优先发 `comment.publish`
3. 同时发热度 `+1`
4. CommentWorker 异步写 `comments`，并更新 `videos.popularity`
5. 发布或删除评论后，删掉 `comment:list:video=<id>` 缓存
6. 解析评论内容中的 `@username`
7. 对提及用户直接写 `notifications` 表

但注意：

- `@提及` 当前是直接写通知表
- 代码中没有把 mention 通过 SSEHub 实时推送出去

也就是说：

- “评论了你的视频”是实时通知
- “评论里提到了你”目前是持久化通知，不是实时 SSE 推送

## 5.3 点赞、评论时的实时通知怎么实现

这个问题建议按“事件复用”来答。

### 点赞实时通知

1. 点赞时发布 `like.like`
2. RabbitMQ 除了业务队列 `like.events`，还有通知队列 `notification.like`
3. NotificationWorker 消费 `notification.like`
4. 根据 `video_id` 反查视频作者
5. 如果作者不是自己，就写一条通知：
   - `recipient_id = 视频作者`
   - `sender_id = 点赞人`
   - `type = like`
   - `target_id = video_id`
6. 如果对方当前在线订阅了 SSE，就通过 `SSEHub.Push` 实时推送给前端

### 评论实时通知

1. 评论时发布 `comment.publish`
2. NotificationWorker 消费 `notification.comment`
3. 根据 `video_id` 找视频作者
4. 写通知并通过 SSE 推送

### 关注实时通知

1. 关注时发布 `social.follow`
2. NotificationWorker 消费 `notification.social`
3. 给被关注用户写一条 `type=follow` 的通知
4. 通过 SSE 推送

### SSE 本身怎么做

SSEHub 做了几件事：

- 内存里维护 `map[userID][]chan *Notification`
- 用户连接 `/notification/stream` 后订阅自己的 channel
- 收到通知时写出 `data: <json>\n\n`
- 每 30 秒发一次 keepalive

所以这是“通知先落库，在线用户再做增量推送”的设计。

---

## 6. Feed 相关问题应该怎么答

## 6.1 最新流怎么做

最新流不是直接每次查数据库，而是冷热分离：

- 热数据走 Redis ZSET `feed:global_timeline`
- 冷数据走 MySQL

发布视频后不会直接同步改 ZSET，而是 outbox + MQ 异步写入。

查询时：

- 先看全局时间线最老那条的分数，得到冷热边界
- 请求游标在热区，就直接查 ZSET
- 请求游标落到冷区，就回 MySQL
- 如果热区页数不够，会拼接一部分冷区数据

这个设计兼顾了：

- 新视频高频访问性能
- 老视频不用长期占 Redis

## 6.2 热榜怎么做

热榜不是直接查 `videos.popularity` 排序这么简单。

优先路径是 Redis：

1. 点赞/评论产生热度增量
2. 写到当前分钟桶 ZSET
3. 查询热榜时，把最近 60 个分钟桶 `ZUNIONSTORE`
4. 生成一个以 `as_of` 为版本号的快照 ZSET
5. 分页时携带相同 `as_of + offset`

这样做能解决两个问题：

- 榜单实时变化导致翻页重复/漏读
- 热点写全打在一个全局 key 上

## 6.3 点赞榜怎么做

点赞榜主要走 MySQL：

- 排序：`likes_count DESC, id DESC`
- 分页游标：`likes_count_before + id_before`

为什么要加 `id_before`：

- 因为很多视频 `likes_count` 可能相同
- 只用点赞数分页会出现重复或漏数据

## 6.4 关注流怎么做

当前代码里的关注流实现非常朴素：

1. 通过 `socials` 查我关注的人
2. 查这些作者的视频
3. 按 `create_time DESC` 排序
4. 结果页缓存到 Redis

它并不是 fanout-on-write 收件箱模型。

所以如果面试官问：

- “你们关注流是推模式还是拉模式？”

你应该答：

- 当前代码落地的是拉模式查询
- 配合 Redis 页面缓存防止重复查库
- 没有看到按粉丝规模切换收件箱/发件箱的完整实现

---

## 7. 这个项目里真实实现的高并发保护手段

可以总结成 6 个点：

1. 三级缓存：本地缓存 + Redis + MySQL
2. 分布式锁：防缓存击穿
3. singleflight：防单机内相同请求并发回源
4. RabbitMQ：异步削峰
5. ZSET 分钟桶：分散热榜写压力
6. Redis 限流：拦截异常高频写请求

如果面试官继续追问“可用性怎么保证”，可以补充：

- token 校验 Redis miss 时回退 MySQL
- 点赞/评论 MQ 失败时同步落库
- outbox 保证视频发布后时间线可补投
- Worker 有重试和死信队列

---

## 8. 简历里最容易被追问出问题的几句话

下面这一部分非常重要。

## 8.1 “基于粉丝规模设计混合动态分发策略：普通用户 fanout-on-write，大 V fanout-on-read”

这句话从当前代码看，不能这么答。

原因：

- 没有看到粉丝规模阈值判断
- 没有看到用户收件箱时间线表
- 没有看到发帖时批量写粉丝时间线
- 关注流当前实现是查 `socials + videos` 再缓存

更安全的表述应该改成：

- “关注流当前采用拉模式查询，结合 Redis 页面缓存；最新流通过 outbox + Redis 全局时间线优化热数据读取。若继续演进，可以按粉丝规模扩展为混合分发。”

这样说，面试官继续追问时你不会露馅。

## 8.2 “RabbitMQ 解耦点赞、评论、关注、热度更新等异步链路”

这句话可以说，但要加限定。

准确表述应该是：

- “点赞、评论、热度更新是 RabbitMQ 异步优先，失败降级同步；关注链路里 MQ 主要承担事件广播和通知扇出，主业务写入仍有同步直写。”

## 8.3 “实现点赞、评论时的实时通知推送”

这句话基本成立，但建议你说更精确：

- 点赞视频作者：实时通知
- 评论视频作者：实时通知
- 关注作者：实时通知
- `@提及`：当前是写通知表，代码中未看到实时 SSE 推送

## 8.4 “Redis 增强高并发场景下稳定性与可用性”

这句话总体成立，但面试里别说成“Redis 挂了完全没事”。

更准确地说：

- token 校验支持 Redis -> MySQL 回退
- 点赞评论支持 Redis/MQ 失败降级
- 但最新流依赖 Redis 全局时间线，Feed 某些路径默认 Redis 在场

## 8.5 “QPS 8k+”

当前仓库里我没有看到：

- 压测脚本
- ab 命令记录
- 压测报告
- 监控截图

所以如果面试官问“8k+ 怎么测出来的、测的是哪个接口、带不带 Redis、带不带 MQ、并发多少、机型是什么”，你如果没有单独材料，风险很大。

更稳妥的做法：

- 要么准备真实压测记录
- 要么把简历写成“在 4核8G 环境下对核心读接口做过压测，具备较高吞吐表现”

## 8.6 “已在线上部署，基于 CI/CD 自动发布”

当前仓库能确认的是：

- 有 Dockerfile
- 有 docker-compose
- 有 start.sh

但没看到明确的 CI/CD 配置文件，比如：

- GitHub Actions
- Jenkinsfile
- GitLab CI

所以如果你没有仓库外的部署配置材料，这句话也要小心。

---

## 9. 面试时推荐你直接背的版本

如果面试官问：“你这个项目里 MySQL、Redis、RabbitMQ 分别怎么用的？”

你可以直接这样答：

“MySQL 是最终业务数据落库，存用户、视频、点赞、评论、关注、通知这些核心表，视频表里还冗余维护了 `likes_count` 和 `popularity`，并针对最新流、点赞榜、热榜做了对应索引。Redis 主要承担缓存和有序集合两部分职责，一方面缓存 token、视频详情、评论列表、Feed 读模型和视频实体，另一方面用 ZSET 维护全局最新流时间线和按分钟分桶的热榜。RabbitMQ 主要做异步解耦和事件扇出，点赞、评论、热度更新是异步优先，Worker 负责落库和更新统计；同一条业务事件还会再扇出到通知队列，最后通过 SSE 做实时通知推送。” 

如果面试官问：“点赞和评论实时通知是怎么做的？”

你可以答：

“点赞、评论、关注都会先发布 RabbitMQ 事件。除了业务 Worker 以外，我又额外绑定了通知队列。NotificationWorker 消费通知队列后，会查出接收人，先把通知落到 `notifications` 表，再通过 SSEHub 给在线用户实时推送。这样可以同时保证离线可追溯和在线实时性。” 

如果面试官问：“Redis 过期怎么设的？”

你可以答：

“不同业务 TTL 不一样：access token 24 小时，refresh token 7 天，视频详情 5 分钟，评论列表和主页缓存 5 秒，Feed 读模型和视频实体 1 小时，热榜分钟桶 2 小时，热榜分页快照 2 分钟。设计思路是：强一致要求高、变化快的缓存 TTL 短；重建成本高、变化没那么剧烈的读模型 TTL 长一些。” 

---

## 10. 最后一句最重要的话

如果面试一定要结合这个项目实话实说，那么你最稳的策略不是把系统吹得特别大，而是把已经写出来的部分讲透：

- 点赞/评论异步化怎么做
- 热榜为什么用分钟桶 ZSET
- 最新流为什么用 outbox + timeline
- token 校验为什么 Redis + MySQL 双层兜底
- 缓存击穿怎么防
- 通知为什么“先落库，再 SSE”

这些都是真正能落到代码细节里的亮点。

反过来，像“混合分发”“8k+ QPS”“CI/CD 已上线”这类，如果没有额外材料支撑，面试里一定要谨慎收口。
